# macOS metadata
.DS_Store

# Claude Code / Claude Scholar internals
.claude/
CLAUDE.md

# Node.js
node_modules/
pnpm-lock.yaml
yarn.lock

# TypeScript incremental cache
tsconfig.tsbuildinfo

# Zotero plugin scaffold
.env
.scaffold/
build/
logs/

# Editor
.vscode/
.idea/
*.swp
*.swo

# Internal design history — kept on disk for reference, not published.
docs/PLAN.md

# Note: typings/i10n.d.ts and prefs.d.ts ARE committed even though they are
# scaffold-generated, so a fresh clone can typecheck before the first build.

# Local scratch space (screenshots, disposable intermediates)
scratch/
